{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for LearnForge. Grants core IPC + shell-open (URL launching in MarkdownRenderer) + dialog-save + fs-write-file (Phase 6 cert/badge export flow per CR-01).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "dialog:allow-save",
    "fs:allow-write-file"
  ]
}
