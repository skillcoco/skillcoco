# Dependencies
node_modules/

# Build
dist/
src-tauri/target/
target/
# Next.js artifacts accidentally produced by tool wrappers (project is Vite, not Next)
.next/
next-env.d.ts

# Environment
.env
.env.local

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Tauri
src-tauri/gen/

# Internal GSD planning workspace — not part of the OSS product
.planning/

# Scratch / working files
scratchpad/
out/

# Confidential / private — must never enter the public OSS repo
# (enforced by .githooks/pre-commit + .github/workflows/secret-scan.yml)
**/private/
pro/
_pro_placeholder/
*.key
id_rsa
id_ed25519
credentials
credentials.*

# Python packaging build artifacts (scripts/ is now pip-installable)
scripts/build/
scripts/*.egg-info/
__pycache__/
*.pyc
